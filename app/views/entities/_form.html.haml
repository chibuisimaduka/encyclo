= form_for(Entity.new) do |f|
  .fields
    = f.hidden_field(:parent_id, :value => parent.id)
    .field_name
      = f.label :name
      = f.autocomplete_field :name, autocomplete_entity_name_entities_path, :id => "create_entity_name_field"
  .actions
    = f.submit "Create Sub-Entity"
